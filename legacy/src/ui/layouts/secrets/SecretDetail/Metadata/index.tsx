import React from 'react';

export const MetaData: React.FC<{
  isExpended?: any;
  secretId: TId;
  filter?: any;
  runId?: any;
  pagination?: boolean;
}> = ({ secretId, filter, isExpended = false }) => {
  return <>Coming soon</>;
};
